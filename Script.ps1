# Terraform

terraform init
terraform validate
terraform plan
terraform apply # type yes

# Output validation
az group show --name example_rg

# Final phase
terraform destroy

# Git operation performed with Visual Studio Code integration. You may use below commands to otherwise:
git init
git remote add origin REPLACE_WITH_GITHUB_REPO_LINK
git add . # Adding all files but excluding .gitignore
git commit -m "REPLACE WITH YOUR COMMIT MESSAGE"
git push